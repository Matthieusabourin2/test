{
  "name": "claude-dashboard-sidecar",
  "version": "0.3.0",
  "description": "Local sidecar that reads Claude Code hook events and exposes a unified view to the dashboard. Uses macOS Accessibility (osascript) to open existing sessions in Claude Desktop.",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=18"
  }
}
